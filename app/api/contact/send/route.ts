import { NextResponse } from 'next/server';import { Resend } from 'resend';export async function POST(req:Request){const {to,subject,message}=await req.json();const resend=new Resend(process.env.RESEND_API_KEY);await resend.emails.send({from:'QuietCircle <onboarding@resend.dev>',to,subject,html:`<p>${message}</p>`});return NextResponse.json({ok:true});}
